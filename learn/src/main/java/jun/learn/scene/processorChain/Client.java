package jun.learn.scene.processorChain;

public class Client {
	public static void main(String[] args) {
		/**
		 * SyncManger manager = new SyncManager();
		 * manager.sync(name, DataProvider)
		 * [
		 * 		List<T> list = new ArrayList<T>;
		 * 		Convertor convertor = new Convertor();
		 * 		while (DataProvider.hasNext()) {
		 * 			T val = convertor.convert(DataProvider.next(), reqClass)
		 * 			list.add(val)
		 * 		}
		 * 
		 * 		Listener<T> listener = (Listener<T>) listeners.get(name);
		 * 		listener.beforeProcess(list);
		 * 	
		 * 		ObjectProcessorChain<T> processor = buildProcessorChain(name, t);
		 * 		for (T t : list) {
		 * 			processor.process(t);
		 * 			[
		 * 				ctx.refresh();
		 * 				ctx.process(t);
		 * 			]
		 * 		}
		 * ]
		 * 
		 * 1. 
		 * 
		 */
	}
}
