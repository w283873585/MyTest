package jun.learn.scene.j2eefilter.version3;

public class Client {
	public static void main(String[] args) {
		/**
		 * request = getRequest();
		 * response = getResponse();
		 * 
		 * FilterChain chain = buildChain(request);
		 * chain.doFilter(request, response);
		 * 
		 * processRealLogic();
		 */
	}
}
